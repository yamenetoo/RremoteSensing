# Raw / large data (download via the pipeline instead of tracking in git)
data/raw/*
!data/raw/.gitkeep

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# Environment
.venv/
venv/

# Large generated outputs (regenerate via pipeline; keep only final CSV/PNG you want to ship)
results/figures/CA_Markov_Outputs/*.tif

# OS / editor
.DS_Store
.ipynb_checkpoints/
